package ParkingLot;

public class Client {
   //System.out.println();
}

// Requirements
// 1. Generate Ticket workflow

//way to think
// 1. which model is it about? - Ticket
// 2. TicketController, TicketService, TicketRepository


//should TicketController methods take models as parameters and return types?
// 1. Db model should not be exposed
// 2. Try to take a single parameter in controller, so that for new parameters the code doesn't break